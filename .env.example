# País para Computrabajo: pe (Perú), co (Colombia), mx (México), ar (Argentina)
PAIS=pe

# Segundos de espera entre requests (no bajar de 1.5)
DELAY_MIN=2.0
DELAY_MAX=4.5

# Reintentos por página fallida
MAX_REINTENTOS=3

# Workers concurrentes (uno por plataforma)
MAX_WORKERS=3
